#!/usr/bin/node
const sentences = [
    'C is fun',
    'Python is cool',
    'JavaScript is amazing'
];

let index = 0;
while (index < sentences.length) {
    console.log(sentences[index]);
    index++;
}